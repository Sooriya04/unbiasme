<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>🧠 Bias of the Day | UnbiasMe</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #f2fbf6;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      .bias-card {
        max-width: 850px;
        margin: 60px auto;
        padding: 40px;
        background-color: #ffffff;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      }

      .bias-card h1 {
        color: #198754;
        font-weight: 700;
        font-size: 2.2rem;
      }

      .bias-card h3 {
        font-size: 1.5rem;
        color: #333;
        margin-top: 30px;
      }

      .bias-card p {
        font-size: 1.1rem;
        color: #555;
        line-height: 1.6;
      }

      .back-home {
        display: inline-block;
        margin-top: 30px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="bias-card">
        <h1 class="text-center">🧠 Bias of the Day</h1>

        <h3><%= bias.name %></h3>
        <hr />

        <p><strong>Definition:</strong> <%= bias.definition %></p>
        <p><strong>Example:</strong> <%= bias.example %></p>
        <p><strong>How to Avoid:</strong> <%= bias.prevention %></p>

        <a href="/" class="btn btn-outline-success back-home"
          >← Back to Homepage</a
        >
      </div>
    </div>
  </body>
</html>
